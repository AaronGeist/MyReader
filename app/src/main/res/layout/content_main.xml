<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="aaron.geist.myreader.activity.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!" />

    <WebView android:id="@+id/webContainer"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:focusable="false"
        android:layout_below="@+id/triggerButton"
        android:layout_alignParentStart="true"
        android:layout_marginTop="63dp" />

    <Button
        android:id="@+id/triggerButton"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Go"
        android:background="#ffffffff"
        android:textColor="#000000"
        android:layout_marginTop="30dp"
        android:layout_alignParentStart="true" />

    <Button
        android:id="@+id/subscribePageBtn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="New feed"
        android:background="#ffffffff"
        android:textColor="#000000"
        android:layout_marginTop="14dp"
        android:layout_below="@+id/triggerButton"
        android:layout_alignParentStart="true" />
</RelativeLayout>
